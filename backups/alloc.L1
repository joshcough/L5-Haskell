; a single array containing only a single 1 (3 is encoded version of 1)
; esi | edi | ebp | esp eax | ecx | edx | ebx
((
  (eax <- (allocate 3 3))
  (edi <- eax)
  ;((mem edi 0) <- 1) ; failed
  (edi += 4) ; failed
  (eax <- (print edi))
  ;(esi <- edi)
  ;(esi += 4)
  ;(edi <- (mem esi 0))
  ;(eax <- (print edi))
))
