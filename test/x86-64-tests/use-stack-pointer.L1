((
  ; set up the stack pointer to get something off the stack, i think...
  (esp -= 32)    ;; save space on the stack for 3 temporaries

  ; put something on the stack
  ((mem esp 0)  <- 3)
  ((mem esp 8)  <- 85)
  ((mem esp 16) <- 109)

  (ebx <- (mem esp 16))
  (eax <- (print ebx))

  (ebx <- (mem esp 8))
  (eax <- (print ebx))

  (esp += 16)
  (ebx <- (mem esp -16))
  (eax <- (print ebx))

  (esp += 16)
))
