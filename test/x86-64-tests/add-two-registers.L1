((
  (rsp -= 8)
  (rsi <- 7) ; rax is 3
  (rdi <- 7) ; rdi is also 3
  (rsi += rdi) ; rdi should be 14 (1110)
  (rsi -= 1) ; when adding two of our encoded numbers, we must subtract 1.
  (rax <- (print rsi)) ; this prints 6 properly
  (rsp += 8)
  (return)
))
