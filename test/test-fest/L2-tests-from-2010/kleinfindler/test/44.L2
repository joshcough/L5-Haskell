(((call :L_1))
 (:L_1 (rcx <- 3) (rdx <- 5) (rax <- 7) (tail-call :f))
 (:f
  (x <- rcx)
  (y <- rdx)
  (z <- rax)
  (rax <- (print x))
  (_ <- rax)
  (rax <- (print y))
  (_ <- rax)
  (rax <- (print z))
  (_ <- rax)
  (rcx <- y)
  (rdx <- z)
  (rax <- x)
  (call :g)
  (_ <- rax)
  (rax <- (print x))
  (_ <- rax)
  (rax <- (print y))
  (_ <- rax)
  (rax <- (print z))
  (rax <- rax)
  (return))
 (:g
  (x <- rcx)
  (y <- rdx)
  (z <- rax)
  (rax <- (print x))
  (_ <- rax)
  (rax <- (print y))
  (_ <- rax)
  (rax <- (print z))
  (rax <- rax)
  (return)))
