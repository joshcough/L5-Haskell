((
  (rax <- 11)
  (call :encode)
  (res <- rax)
  (rax <- (print res))
  )
 (:encode
  (x <- rax)
  (rax <- x)
  (call :times2)
  (t <- rax)
  (rax <- t)
  (tail-call :add1))
 (:times2
  (x <- rax)
  (x >>= 1)
  (x *= 2)
  (x <<= 1)
  (x += 1)
  (rax <- x)
  (return))
 (:add1
  (x <- rax)
  (x += 3)
  (x -= 1)
  (rax <- x)
  (return))
)