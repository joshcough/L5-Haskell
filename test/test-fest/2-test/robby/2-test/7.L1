(((call :L_1))
 (:L_1
  (rbx <- rsi)
  (rdi <- rdi)
  (rsi <- 21)
  (rsi <- rsi)
  (rsi &= 1)
  (rsi *= -2)
  (rsi += 3)
  (rax <- (print rsi))
  (rax <- rax)
  (rsi <- rbx)
  (rdi <- rdi)
  (return)))
