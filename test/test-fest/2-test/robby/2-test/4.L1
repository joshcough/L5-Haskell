(((call :L_1))
 (:L_1
  (rbx <- rsi)
  (rdi <- rdi)
  (rax <- (allocate 3 3))
  (rsi <- rax)
  (rsi <- rsi)
  (rsi &= 1)
  (rsi <<= 1)
  (rsi += 1)
  (rax <- (print rsi))
  (rax <- rax)
  (rsi <- rbx)
  (rdi <- rdi)
  (return)))
