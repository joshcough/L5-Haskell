(((call :L_1))
 (:L_1
  (rbx <- rsi)
  (rdi <- rdi)
  (rax <- (allocate 21 17))
  (rsi <- rax)
  (rsi <- (mem rsi 0))
  (rsi <<= 1)
  (rsi += 1)
  (rax <- (print rsi))
  (rax <- rax)
  (rsi <- rbx)
  (rdi <- rdi)
  (return)))
