(((call :L_1))
 (:L_1
  (rsi <- rsi)
  (rdi <- rdi)
  (rcx <- 3)
  (rdx <- 5)
  (call :f)
  (rbx <- rax)
  (rax <- (print rbx))
  (rax <- rax)
  (rsi <- rsi)
  (rdi <- rdi)
  (return))
 (:f
  (rbx <- rsi)
  (rdi <- rdi)
  (rax <- rcx)
  (rsi <- rdx)
  (rax <- rax < rsi)
  (rax += rax)
  (rax += 1)
  (cjump rax = 1 :L_3 :L_2)
  :L_3
  (rax <- 5)
  (rsi <- rbx)
  (rdi <- rdi)
  (return)
  :L_2
  (rax <- 3)
  (rsi <- rbx)
  (rdi <- rdi)
  (return)))
