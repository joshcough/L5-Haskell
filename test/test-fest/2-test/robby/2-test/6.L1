(((call :L_1))
 (:L_1
  (rbx <- rsi)
  (rdi <- rdi)
  (rax <- (allocate 5 0))
  ((mem rax 4) <- 21)
  ((mem rax 8) <- 23)
  (rsi <- rax)
  (rsi <- rsi)
  (rsi &= 1)
  (rsi <<= 1)
  (rsi += 1)
  (rax <- (print rsi))
  (rax <- rax)
  (rsi <- rbx)
  (rdi <- rdi)
  (return)))
