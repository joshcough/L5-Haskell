(((call :L_1))
 (:L_1
  (rbx <- rsi)
  (rdi <- rdi)
  (rax <- (allocate 21 23))
  (rsi <- rax)
  (rax <- -1)
  (rdx <- (mem rsi 0))
  (rdx <<= 1)
  (rdx += 1)
  (cjump rax < rdx :oka_5 :die_4)
  :oka_5
  (cjump 0 <= rax :okb_6 :die_4)
  :die_4
  (rax <- (array-error rsi -1))
  :okb_6
  (rax >>= 1)
  (rax *= 4)
  (rax += rsi)
  ((mem rax 4) <- 643)
  (rax <- 1)
  (rsi <- rbx)
  (rdi <- rdi)
  (return)))
