(((call :L_1))
 (:L_1
  (rbx <- rsi)
  (rdi <- rdi)
  (rsi <- 5)
  (rsi >>= 1)
  (rax <- 7)
  (rax >>= 1)
  (rax *= rsi)
  (rax += rax)
  (rax += 1)
  (rax <- (print rax))
  (rax <- rax)
  (rsi <- rbx)
  (rdi <- rdi)
  (return)))
