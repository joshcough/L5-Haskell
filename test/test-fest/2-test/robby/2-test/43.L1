(((call :L_1))
 (:L_1
  (esp += -16)
  (rbx <- rsi)
  (rdi <- rdi)
  (rcx <- 3)
  (rsi <- 5)
  (rax <- 7)
  (rdx <- 9)
  ((mem ebp -16) <- 11)
  ((mem ebp -12) <- 13)
  ((mem ebp -8) <- 15)
  ((mem ebp -4) <- rcx)
  (rcx <- (mem ebp -4))
  (rcx += rsi)
  ((mem ebp -4) <- rcx)
  (rsi <- (mem ebp -4))
  (rsi -= 1)
  ((mem ebp -4) <- rsi)
  (rsi <- (mem ebp -4))
  (rsi += rax)
  ((mem ebp -4) <- rsi)
  (rsi <- (mem ebp -4))
  (rsi -= 1)
  ((mem ebp -4) <- rsi)
  (rsi <- (mem ebp -4))
  (rsi += rdx)
  ((mem ebp -4) <- rsi)
  (rsi <- (mem ebp -4))
  (rsi -= 1)
  ((mem ebp -4) <- rsi)
  (rsi <- (mem ebp -4))
  (rax <- (mem ebp -16))
  (rsi += rax)
  ((mem ebp -4) <- rsi)
  (rsi <- (mem ebp -4))
  (rsi -= 1)
  ((mem ebp -4) <- rsi)
  (rsi <- (mem ebp -4))
  (rax <- (mem ebp -12))
  (rsi += rax)
  ((mem ebp -4) <- rsi)
  (rsi <- (mem ebp -4))
  (rsi -= 1)
  ((mem ebp -4) <- rsi)
  (rsi <- (mem ebp -4))
  (rax <- (mem ebp -8))
  (rsi += rax)
  ((mem ebp -4) <- rsi)
  (rsi <- (mem ebp -4))
  (rsi -= 1)
  ((mem ebp -4) <- rsi)
  (rsi <- (mem ebp -4))
  (rax <- (print rsi))
  (rax <- rax)
  (rsi <- rbx)
  (rdi <- rdi)
  (return)))
