(((call :L_1))
 (:L_1
  (rbx <- rsi)
  (rdi <- rdi)
  (rax <- (allocate 9 0))
  ((mem rax 4) <- 3)
  ((mem rax 8) <- 5)
  ((mem rax 12) <- 7)
  ((mem rax 16) <- 9)
  (rsi <- rax)
  (rsi <- (mem rsi 0))
  (rsi <<= 1)
  (rsi += 1)
  (rax <- (print rsi))
  (rax <- rax)
  (rsi <- rbx)
  (rdi <- rdi)
  (return)))
