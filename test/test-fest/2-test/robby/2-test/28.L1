(((call :L_1))
 (:L_1
  (rsi <- rsi)
  (rdi <- rdi)
  (rcx <- 1)
  (call :f)
  (rbx <- rax)
  (rax <- (print rbx))
  (rax <- rax)
  (rsi <- rsi)
  (rdi <- rdi)
  (return))
 (:f
  (rsi <- rsi)
  (rdi <- rdi)
  (rbx <- rcx)
  (rax <- rbx)
  (rsi <- rsi)
  (rdi <- rdi)
  (return)))
