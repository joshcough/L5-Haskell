(((call :L_1))
 (:L_1
  (rbx <- rsi)
  (rdi <- rdi)
  (rax <- (allocate 9 0))
  ((mem rax 4) <- 3)
  ((mem rax 8) <- 5)
  ((mem rax 12) <- 7)
  ((mem rax 16) <- 9)
  (rsi <- rax)
  (rax <- 7)
  (rdx <- (mem rsi 0))
  (rdx <<= 1)
  (rdx += 1)
  (cjump rax < rdx :oka_5 :die_4)
  :oka_5
  (cjump 0 <= rax :okb_6 :die_4)
  :die_4
  (rax <- (array-error rsi 7))
  :okb_6
  (rax >>= 1)
  (rax *= 4)
  (rax += rsi)
  (rsi <- (mem rax 4))
  (rax <- (print rsi))
  (rax <- rax)
  (rsi <- rbx)
  (rdi <- rdi)
  (return)))
