(((call :alloc)
  (rsi <- rax)
  (rax <- (mem rsi 0))
  (call :printnum)
  (rax <- (mem rsi 4))
  (call :printnum))
 (:alloc
  (rax <- (allocate 7 7))
  (return))
 (:printnum
  (rax *= 2)
  (rax += 1)
  (rax <- (print rax))
  (return)))
