;; allocate
(((rcx <- 13)
  (rdx <- 19)
  (rax <- 3)
  (rdi <- rdx)
  (rsi <- rax)
  (rax <- (allocate rcx 1))
  (rbx <- rax)
  (rax <- (allocate rdi 1))
  ((mem rbx 8) <- 27)
  ((mem rax 8) <- rsi)
  (rax <- (print rax))
  (rax <- (print rbx))
  (rax <- 0)))
