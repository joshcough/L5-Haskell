(((rsi <- 9)
  (rdi <- 11)
  (rax <- (allocate rsi rdi))
  (rax <- (print rax))
  (rsi <- 13)
  (rdi <- 15)
  (rax <- (allocate rsi rdi))
  (rax <- (print rax))
  (rax <- (allocate rdi rsi))
  (rax <- (print rax))
  (rax <- (allocate rsi rsi))
  (rax <- (print rax))))