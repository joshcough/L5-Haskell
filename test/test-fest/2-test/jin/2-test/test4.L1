(((rax <- 9)
  (call :fac)
  (rax <- (print rax)))
 (:fac
  (rbx <- rax)
  (cjump rbx = 1 :base :recure)
  :base
  (rax *= 1)
  (return)
  :recure
  (rdx <- rax)
  (rax -= 1)
  (rax *= rdx)
  (rbx -= 1)
  (rax -= 1)
  (return))) 