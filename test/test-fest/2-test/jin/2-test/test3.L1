(((rax <- 9)
  (call :fun)
  (rax <- (print rax)))
 (:fun
 (rcx <- rax)
  :loop
  (rbx <- 1)
  (cjump rcx < rbx :keep_going :done)
  
  :keep_going
  (rbx -= 1)
  (goto :loop)
  
  :done
  (rcx += 4)
  (rax <- rcx)
  (return)) )