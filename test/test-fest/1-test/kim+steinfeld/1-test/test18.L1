((
(rsi <- 9)
(rax <- (allocate rsi 1))
(rdi <- rax)         ;; base pointer
(rdi += 4)

((mem rdi 0) <- 51)  ;; store new values
((mem rdi 8) <- 37)

(rcx <- (mem rdi 0))
(rdx <- (mem rdi 8))

(rcx += rdx)
(rcx -= 1)
((mem rdi 0) <- rcx)

(rdx <- (mem rdi 0))
(rax <- (print rdx))
))
