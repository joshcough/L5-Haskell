(
 (
  (rax <- 11)    ;; factorial argument
  (rsi <- 1)	 ;; accumulation
  (call :factorial)
  (rsi *= 2)
  (rsi += 1)
  (rax <- (print rsi))
 )

 (:factorial
  (cjump rax < 2 :base :recur)

  :base
  (return)

  :recur
  (rsi *= rax)
  (rax -= 1)
  (tail-call :factorial)
 )
)
