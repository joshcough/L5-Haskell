((
(rsi <- 21)  ;; loop termination
(rbx <- 1)   ;; counter
(rdi <- 1)   ;; result

:loop
(cjump rbx < rsi :keep_going :done)
:keep_going

;; add 2 
(rdx <- rdi)
(rdx += 4)
(rdi <- rdx)

;; increase counter
(rbx += 2)

(goto :loop)

:done
(rax <- (print rdi))
))
