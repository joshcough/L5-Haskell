(
 (
  (rcx <- 7)
  (rax <- (allocate rcx 5))
  ((mem rax 4) <- 13)
  ((mem rax 8) <- 19)
  ((mem rax 12) <- 29)
  (rcx <- 7)
  (call :squarearray)
  (rax <- (print rax))
 )

 (:squarearray
  (rsi <- 3)

  :loop
  (cjump rsi <= rcx :if :end)

  :if
  (rdi <- rsi)
  (rdi -= 1)
  (rdi >>= 1)
  (rdi *= 4)
  (rdi += rax)

  (rbx <- (mem rdi 0))
  (rbx -= 1)
  (rbx >>= 1)
  (rbx *= rbx)
  (rbx <<= 1)
  (rbx += 1)
  ((mem rdi 0) <- rbx)

  (rsi += 2)
  (goto :loop)

  :end
  (return)
 )
)
