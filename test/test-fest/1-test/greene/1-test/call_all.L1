(((rax <- 93)
  (call :fun2)
  (rax <- (print rax))
  (rax <- 93)
  (call :fun0)
  (rax <- (print rax))
  (rax <- 93)
  (call :fun1)
  (rax <- (print rax)))
 (:fun0
  (rax += 62)
  (return))
 (:fun1
  (rax -= 566)
  (return))
 (:fun2
  (rax -= 2)
  (return)))
