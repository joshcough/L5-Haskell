(((rax <- 65)
  (rbx <- 15)
  (goto :loop2)
  :loop1
  (rax += 327)
  (rax -= 1)
  :loop2
  (rax -= 3)
  (rax += 1)
  (rbx -= 3)
  (rbx += 1)
  (cjump 1 = rbx :end :loop2)
  :end
  (rax <- (print rax))))
