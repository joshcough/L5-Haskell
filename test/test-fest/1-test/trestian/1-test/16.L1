((
(rdi <- 3)
(rax <- (print rdi))

(rsi <- 71)
(rbx <- 13)

:loop
(cjump rbx < rsi :keep_going :done)

:keep_going
(rdi <<= 1)
(rdi += 1)
;;(rax <- (print rdi))
(rbx += 2)

(goto :loop)
:done
(rax <- (print rdi))
))
