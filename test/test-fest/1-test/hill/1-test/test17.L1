;; #17 tail call indirect function
(((rsi <- :first_fn)
  (call rsi)
  (rax <- (print rax)))
  
 (:first_fn
  (rsi <- 16)
  (rsi <<= 1)
  (rsi += 1)
  (rax <- (print rsi))
  (rcx <- :second_fn)
  (tail-call rcx))
 
 (:second_fn
  (rsi <- 12)
  (rsi <<= 1)
  (rsi += 1)
  (rax <- (print rsi))
  (rax <- -201)
  (return)))


