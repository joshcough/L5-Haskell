;; #13 stack memory usage
(((rax <- esp)
  (esp -= 32)
  ((mem esp 0) <- 17)
  ((mem esp 4) <- 19)
  ((mem esp 8) <- 21)
  ((mem esp 12) <- 33)
  (rsi <- 0)
  :loop_top
  (rsi += 1)
  (rax <- (mem esp 0))
  (rax <- (print rax))
  (rax <- (mem esp 4))
  (rax <- (print rax))
  (rax <- (mem esp 8))
  (rax <- (print rax))
  (rax <- (mem esp 12))
  (rax <- (print rax))
  (cjump rsi < 3 :loop_top :loop_fin)
  :loop_fin
  (esp += 32)))


