;; #9 cjump with less than
((:LOOP1_SETUP
  (rax <- 1)
  (rcx <- 21)
  :LOOP1_TOP
  (rax += 2)
  (cjump rax < rcx :LOOP1_TOP :LOOP1_FIN)
  (rsi <- 7)
  (rax <- (print rsi))
  :LOOP1_FIN
  (rax <- (print rax))))


