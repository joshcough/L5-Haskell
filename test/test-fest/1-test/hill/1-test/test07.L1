;; #7 allocate, heap mem write, heap mem read
(((rsi <- 25)
  (rax <- (allocate rsi 1))
  (rsi <- rax)
  (rcx <- 9)
  (rdx <- 11)
  (rdi <- 47)
  ((mem rsi 0) <- rcx)
  ((mem rsi 4) <- rdx)
  ((mem rsi 8) <- rdi)
  (rax <- (mem rsi 0))
  (rax <- (print rax))
  (rax <- (mem rsi 4))
  (rax <- (print rax))
  (rax <- (mem rsi 8))
  (rax <- (print rax))
  (rax <- (allocate 9 1))
  ((mem rax 0) <- 121)
  (rax <- (mem rax 0))
  (rax <- (print rax))))


