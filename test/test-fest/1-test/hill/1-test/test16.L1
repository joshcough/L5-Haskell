;; #16 tail call function
(((rbx <- 17)
  (call :first_fn)
  (rax <- (print rax)))
  
 (:first_fn
  (rsi <- 11)
  (rsi <<= 1)
  (rsi += 1)
  (rax <- (print rsi))
  (tail-call :second_fn))
 
 (:second_fn
  (rsi <- 19)
  (rsi <<= 1)
  (rsi += 1)
  (rax <- (print rsi))
  (rax <- 49)
  (return)))


