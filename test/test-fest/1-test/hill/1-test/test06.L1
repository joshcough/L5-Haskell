;; #6 equal to
(((rbx <- 11)
  (rsi <- 11)
  (rdi <- -17)
  (rax <- rbx = rsi)
  (rcx <- rbx = rdi)
  (rdx <- rdi = rbx)
  (rbx <- rax)
  (rsi <- rcx)
  (rdi <- rdx)
  (rbx <<= 1)
  (rbx += 1)
  (rax <- (print rbx))
  (rsi <<= 1)
  (rsi += 1)
  (rax <- (print rsi))
  (rdi <<= 1)
  (rdi += 1)
  (rax <- (print rdi))))


