;; #23 recursive, non tail-call function calls, with stack
(((rax <- 12)
  (call :FN_A))
  
 (:FN_A
  (esp -= 4)
  ((mem esp 0) <- rax)
  (rcx <- rax)
  (rcx <<= 1)
  (rcx += 1)
  (rax <- (print rcx))
  (rax <- (mem esp 0))
  (cjump 0 < rax :GO :QUIT)
  :GO
  (rax -= 1)
  (call :FN_A)
  (esp += 4)
  (return)
  :QUIT
  (esp += 4)
  (return)))


