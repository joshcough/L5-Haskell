;; #11 cjump with equals
((:LOOP3_SETUP
  (rbx <- 64)
  :LOOP3_TOP
  (rbx >>= 1)
  (cjump rbx = 0 :LOOP3_FIN :LOOP3_TOP)
  (rcx <- 11)
  (rax <- (print rcx))
  :LOOP3_FIN
  (rbx += 25)
  (rax <- (print rbx))))


