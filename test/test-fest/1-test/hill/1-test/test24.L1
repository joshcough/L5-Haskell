;; #24 coroutines
(((rax <- 8)
  (call :FN_A))
  
 (:FN_A
  (rdi <- rax)
  (rcx <- rax)
  (rcx <<= 1)
  (rcx += 1)
  (rax <- (print rcx))
  (rax <- rdi)
  (cjump 0 < rax :GO_A :QUIT_A)
  :GO_A
  (rax -= 2)
  (call :FN_B)
  (return)
  :QUIT_A
  (return))
 
 (:FN_B
  (rdi <- rax)
  (rcx <- rax)
  (rcx <<= 1)
  (rcx += 1)
  (rax <- (print rcx))
  (rax <- rdi)
  (cjump 0 < rax :GO_B :QUIT_B)
  :GO_B
  (rax += 1)
  (call :FN_A)
  (return)
  :QUIT_B
  (return))))