;; #8 labels, goto
((:START
  (rax <- 9)
  (rcx <- 3)
  (goto :PART2)
  :PART1
  (rax *= rcx)
  (goto :END)
  :PART2
  (rax -= rcx)
  (goto :PART1)
  :END
  (rax <<= 1)
  (rax += 1)
  (rax <- (print rax))))


