;; #5 less than or equal to
(((rbx <- 5)
  (rsi <- 5)
  (rdi <- -2)
  (rax <- rbx <= rsi)
  (rcx <- rbx <= rdi)
  (rdx <- rdi <= rbx)
  (rbx <- rax)
  (rsi <- rcx)
  (rdi <- rdx)
  (rbx <<= 1)
  (rbx += 1)
  (rax <- (print rbx))
  (rsi <<= 1)
  (rsi += 1)
  (rax <- (print rsi))
  (rdi <<= 1)
  (rdi += 1)
  (rax <- (print rdi))))


