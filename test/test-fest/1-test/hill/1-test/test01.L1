;; #1 assignment, limits, addition
(((rsi <- 29)
  (rdi <- -71)
  (rax <- 0)
  (rax += rsi)
  (rax += rsi)
  (rax += rdi)
  (rax <- (print rax))
  (rcx <- 0)
  (rcx += 31)
  (rax <- (print rcx))
  (rdx <- 0)
  (rdx += 47)
  (rax <- (print rdx))))


