;; #14 functions
(((rcx <- 21)
  (call :some_fn)
  (rax <- (print rax))
  (call :another_fn)
  (rax <- (print rax)))
 
 (:some_fn
  (rax <- 19)
  (return))
 
 (:another_fn
  (rax <- 45)
  (return)))


