;; #2 arith ops
(((rsi <- 27)
  (rdi <- 14)
  (rax <- rsi)
  (rax += rdi)
  (rax <<= 1)
  (rax += 1)
  (rax <- (print rax))
  (rax <- rsi)
  (rax -= rdi)
  (rax <<= 1)
  (rax += 1)
  (rax <- (print rax))
  (rax <- rsi)
  (rax *= rdi)
  (rax <<= 1)
  (rax += 1)
  (rax <- (print rax))
  (rax <- rsi)
  (rax &= rdi)
  (rax <<= 1)
  (rax += 1)
  (rax <- (print rax))))


