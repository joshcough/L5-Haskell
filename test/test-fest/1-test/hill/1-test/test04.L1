;; #4 less than
(((rbx <- 9)
  (rsi <- 9)
  (rdi <- -15)
  (rax <- rbx < rsi)
  (rcx <- rbx < rdi)
  (rdx <- rdi < rbx)
  (rbx <- rax)
  (rsi <- rcx)
  (rdi <- rdx)
  (rbx <<= 1)
  (rbx += 1)
  (rax <- (print rbx))
  (rsi <<= 1)
  (rsi += 1)
  (rax <- (print rsi))
  (rdi <<= 1)
  (rdi += 1)
  (rax <- (print rdi))))


