;; #12 label case sensitivity
(((rax <- 5)
  (rbx <- 5)
  (cjump rax = rbx :GOOD_PLACE :good_place)
  :good_place
  (rax <- 7)
  (rax <<= 1)
  (rax += 1)
  (rax <- (print rax))
  :GOOD_PLACE
  (rax <- 13)
  (rax <<= 1)
  (rax += 1)
  (rax <- (print rax))))


