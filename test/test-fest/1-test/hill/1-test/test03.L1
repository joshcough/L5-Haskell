;; #3 shift ops
(((rsi <- 15)
  (rsi >>= 1)
  (rax <- (print rsi))
  (rsi <- 15)
  (rsi <<= 1)
  (rsi += 1)
  (rax <- (print rsi))
  (rsi <- -64)
  (rsi >>= 3)
  (rsi -= 1)
  (rax <- (print rsi))
  (rsi <- -64)
  (rsi <<= 3)
  (rsi += 1)
  (rax <- (print rsi))))


