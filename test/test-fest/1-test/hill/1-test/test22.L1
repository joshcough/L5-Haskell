;; #22 recursive, tail-call function calls
(((rax <- 15)
  (call :FN_A))
  
 (:FN_A
  (rsi <- rax)
  (rcx <- rax)
  (rcx <<= 1)
  (rcx += 1)
  (rax <- (print rcx))
  (rax <- rsi)
  (cjump 0 < rax :GO :QUIT)
  :GO
  (rax -= 1)
  (tail-call :FN_A)
  :QUIT
  (return)))


