;; #10 cjump with less than or equals
((:LOOP2_SETUP
  (rsi <- -1)
  (rdi <- -32)
  (rax <- 2)
  :LOOP2_TOP
  (rsi *= rax)
  (cjump rdi <= rsi :LOOP2_TOP :LOOP2_FIN)
  (rcx <- 9)
  (rax <- (print rcx))
  :LOOP2_FIN
  (rsi <<= 1)
  (rsi += 1)
  (rax <- (print rsi))))


