;; #20 cjump using result of comparison
(((rdx <- 301 < 501)
  (rdi <- rdx)
  (cjump rdi < 0 :a :a)
  (rax <- (print 25))
  :a
  (cjump rdi <= 0 :b :b)
  (rax <- (print 39))
  :b
  (cjump rdi = 0 :c :c)
  (rax <- (print 35))
  :c
  (cjump 1 = rdi :d :d)
  (rax <- (print 81))
  :d
  (rax <- (print 95))))


