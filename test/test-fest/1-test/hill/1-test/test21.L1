;; #21 recursive, non tail-call function calls
(((rax <- 10)
  (call :FN_A))
  
 (:FN_A
  (rdi <- rax)
  (rcx <- rax)
  (rcx <<= 1)
  (rcx += 1)
  (rax <- (print rcx))
  (rax <- rdi)
  (cjump 0 < rax :GO :QUIT)
  :GO
  (rax -= 1)
  (call :FN_A)
  (return)
  :QUIT
  (return)))


