;test case 21
(((rax <- 18)
  (call :add)
  (rax -= 2)
  (rax <- (print rax)))
 (:add
  (cjump rax < 20 :add1 :add2)
  :add1
  (rax += 1)
  (return)
  :add2
  (call :sub)
  (rax += 2)
  (return))
 (:sub
  (rax -= 10)))