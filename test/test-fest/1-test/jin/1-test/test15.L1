;test case 15
(((rsi <- 21)
  (rax <- (allocate rsi 3))
  (rdi <- rax)
  (rbx <- rdi)
  (rbx += 12)
  (rsi *= 2)
  (rsi += 2)
  (rsi += rdi)
  (rcx <- 1)
  :loop
  (cjump rbx < rsi :keep_going :done)
  :keep_going
  (cjump rcx = 1 :step_1 :step_2)
  :step_1
  (rdx <- (mem rbx 0))
  (rdx += 2)
  ((mem rbx -4) <- rdx)
  (rdx += 2)
  ((mem rbx 0) <- rdx)
  (rbx += 4)
  (rcx += 1)
  (goto :loop)
  :step_2
  (rdx <- (mem rbx -4))
  (rdx += 2)
  ((mem rbx 0) <- rdx)
  (rbx += 4)
  (goto :loop)
  :done
  (rax <- (print rdi))))