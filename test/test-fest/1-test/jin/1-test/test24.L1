;test case 7
(((rsi <- 21)
  (rax <- (allocate rsi 3))
  (rdi <- rax)
  (rbx <- rdi)
  (rbx += 8)
  (rsi *= 2)
  (rsi += 2)
  (rsi += rdi)
  :loop
  (cjump rbx < rsi :keep_going :done)
  :keep_going
  (rdx <- (mem rbx -4))
  (rdx += 2)
  ((mem rbx 0) <- rdx)
  (rbx += 4)
  (goto :loop)
  :done
  
  (rax <- (print rdi))
  (rax <- rdi)
  (rsi <- 21)
  (rbx <- rax)
  (rbx += 8)
    (rsi *= 2)
  (rsi += 2)
  (rsi += rdi)
  :loop1
  (cjump rbx < rsi :keep_going1 :done1)
  :keep_going1
  (rdx <- (mem rbx -4))
  (rcx <- (mem rbx 0))
  (rdx -= rcx)
  (rdx += 1)
  ((mem rbx 0) <- rdx)
  (rbx += 4)
  (goto :loop1)
  :done1
  (rax <- (print rdi))))