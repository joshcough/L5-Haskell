;test case 13
(((rax <- 8)
  (call :fib)   
  (rax += rax)
  (rax += 1)
  (rax <- (print rax)))
 (:fib
  (esp -= 8)   
  (cjump rax < 2 :base :recur)
  :base
  (rax <- 1)
  (esp += 8)     
  (return)       
  :recur        
  ((mem ebp -4) <- rax)
   (rax -= 1)
  (call :fib)    
  ((mem ebp -8) <- rax)
  (rax <- (mem ebp -4))
  (rax -= 2)
  (call :fib)    
  (rsi <- (mem ebp -8))
  (rax += rsi)
  (esp += 8)     
  (return)))