;test case 20
(((rsi <- 31)
  (rax <- (allocate rsi 57))
  (rax <- (print rax))
  (rax <- (allocate rsi 57))
  (rdi <- rax)
  (rbx <- rdi)
  (rbx += 12)
  (rsi *= 2)
  (rsi += 2)
  (rsi += rdi)
  :loop
  (cjump rbx < rsi :keep_going :done)
  :keep_going
  (rdx <- (mem rbx -4))
  (rdx += 14)
  ((mem rbx 0) <- rdx)
  (rbx += 4)
  (goto :loop)
  :done
  (rax <- (print rdi))))