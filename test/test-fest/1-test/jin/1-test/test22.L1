;test case 22
(((rax <- 16)
  (call :mul)
  (rax -= 1)
  (rax <- (print rax)))
 (:mul
  (cjump rax < 10 :mul1 :mul2)
  :mul1
  (rax *= 2)
  (return)
  :mul2
  (rcx <- 1)
  :loop  
  (cjump rcx < 5 :times :result)
  :times
  (rax *= rcx)
  (rcx += 1)
  (goto :loop)
  :result
  (return)))