;test case 12
((
  (rsi <- 21)
  (rax <- (allocate rsi 3))
  (rdi <- rax)
  (rbx <- rdi)
  (rbx += 12)
  (rsi *= 2)
  (rsi += 2)
  (rsi += rdi)
  :loop
  (esp -= 8)
  (cjump rbx < rsi :keep_going :done)
  :keep_going
  (rdx <- (mem rbx -4))
  (rdx -= 1)
  ((mem ebp -4) <- rdx)
  (rdx += 1)
  (rdx *= rdx)
  ((mem ebp -8) <- rax)
  (rax <- (mem ebp -4))
  (rax *= rax)
  (rdx -= rax)
  (rax <- (mem ebp -8))
  ((mem rbx 0) <- rdx)
  (rbx += 4)
  (esp += 8)
  (goto :loop)
  :done
  (rax <- (print rdi))
  (esp += 8)))
