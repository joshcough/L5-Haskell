;test case 23
(((rsi <- 21)
  (rax <- (allocate rsi 3))
  (rdi <- rax)
  (rbx <- rdi)
  (rbx += 12)
  (rsi *= 2)
  (rsi += 2)
  (rsi += rdi)
  :loop
  (cjump rbx < rsi :keep_going :done)
  :keep_going
  (rdx <- (mem rbx -4))
  (rcx <- (mem rbx 0))
  (rdx *= rcx)
  ((mem rbx 0) <- rdx)
  (rbx += 4)
  (goto :loop)
  :done
  (rax <- (print rdi))))