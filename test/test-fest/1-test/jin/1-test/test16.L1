;test case 16
(((rsi <- 21)
  (rax <- (allocate rsi 3))
  (rdi <- rax)
  (rbx <- rdi)
  (rbx += 12)
  (rsi *= 2)
  (rsi += 2)
  (rsi += rdi)
  (rcx <- 15)
  (call :fib)
  (rax <- (print rax)))
 (:fib
  :loop
  (cjump rbx < rsi :keep_going :done)
  :keep_going
  (cjump rcx < 6 :base :recure)
  :base
  (rdx <- (mem rbx -4))
  (rdx *= 2)
  (rdx += 1)
  ((mem rbx 0) <- rdx)
  (rbx += 4)
  :recure
  (rcx -= 1)
  (call :fib)
  (goto :loop)
  :done
  (return)))