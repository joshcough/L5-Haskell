;test case 19
(((rax <- 10)
  (rbx <- rax)
  (call :fac)
  (rax <- (print rax)))
  (:fac
   (cjump rbx = 1 :base :recure)
   :base
   (rax *= 1)
   (return)
   :recure
   (rdx <- rax)
   (rax -= 1)
   (rax *= rdx)
   (rbx -= 1)
   (rax -= 1)
   (return)))