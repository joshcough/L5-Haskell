;test case 11
(((rsi <- 21)
  (rax <- (allocate rsi 21))
  (rdi <- rax)
  (rbx <- 5)
  :loop
  (cjump rbx < rsi :keep_going :done)
  :keep_going
  (rdx <- rbx)
  (rdx -= 2)
  (rdx -= 1)
  (rdx *= 2)
  (rdx += rdi)
  (rdx += 4)
  (rdx <- (mem rdx 0))
  (rdx -= 2)
  (rcx <- rbx)
  (rcx -= 1)
  (rcx *= 2)
  (rcx += 4)
  (rcx += rdi)
  ((mem rcx 0) <- rdx)
  (rbx += 2)
  (goto :loop)
  :done
  (rax <- (print rdi))))