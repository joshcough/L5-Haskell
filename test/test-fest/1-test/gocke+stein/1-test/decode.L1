(((rax <- 10)
  (call :decode)
  (rax <- (print rax))
  (rax <- 5)
  (call :decode)
  (rax <- (print rax))
  (rax <- 0)
  (call :decode)
  (rax <- (print rax))
  (rax <- -10)
  (call :decode)
  (rax <- (print rax)))
 (:decode
  (rax *= 2)
  (rax += 1)
  (return)))
