(((rax <- 72)
  (rbx <- rax)
  (rcx <- rax)
  (rdx <- rax)
  (rsi <- rax)
  (rdi <- rax)

  (rbx += 1)

  (rax <- rbx)
  (rcx <- rbx)
  (rdx <- rbx)
  (rsi <- rbx)
  (rdi <- rbx)
  
  (rcx += 1)

  (rax <- rcx)
  (rbx <- rcx)
  (rdx <- rcx)
  (rsi <- rcx)
  (rdi <- rcx)

  (rdx += 1)

  (rax <- rdx)
  (rbx <- rdx)
  (rcx <- rdx)
  (rsi <- rdx)
  (rdi <- rdx)

  (rsi += 1)

  (rax <- rsi)
  (rbx <- rsi)
  (rcx <- rsi)
  (rdx <- rsi)
  (rdi <- rsi)

  (rdi += 1)

  (rax <- rdi)
  (rbx <- rdi)
  (rcx <- rdi)
  (rdx <- rdi)
  (rsi <- rdi)

  (rax *= 2)
  (rax += 1)

  (rbx *= 2)
  (rbx += 1)

  (rcx *= 2)
  (rcx += 1)

  (rdx *= 2)
  (rdx += 1)

  (rsi *= 2)
  (rsi += 1)

  (rdi *= 2)
  (rdi += 1)

  (rax <- (print rax))
  (rax <- (print rsi))
  (rax <- (print rdi))))
