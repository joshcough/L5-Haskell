(((rax <- (allocate 17 1))
  (rdx <- 27) ;; store D
  ((mem rax 4) <- rdx)
  ((mem rax 16) <- rdx)
  (rdx <- 29) ;; store E
  ((mem rax 8) <- rdx)
  ((mem rax 24) <- rdx)
  ((mem rax 28) <- rdx)
  ((mem rax 12) <- 21) ;; Store A
  ((mem rax 20) <- 23) ;; Store B
  ((mem rax 32) <- 31) ;; Store F
  (rax <- (print rax))))
