;; Create a long tail recursion -- shouldn't die
(((rax <- 10000)
  (call :recur_not_recurse)
  (rax *= 2)
  (rax += 1)
  (rax <- (print rax)))
 (:recur_not_recurse
  (cjump rax = 0 :ret :recur)
  :ret
  (return)
  :recur
  (rax -= 1)
  (tail-call :recur_not_recurse)))
