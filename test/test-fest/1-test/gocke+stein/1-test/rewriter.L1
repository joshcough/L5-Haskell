(((rax <- (allocate 9 1))
  (rdx <- rax)
  (rax <- 1024)
  (rbx <- 0)

  :loop
  (rbx += rax)
  ((mem rdx 0) <- rbx)
  (rax -= 4)
  (cjump rax < 0 :finish :loop)

  :finish
  (rcx <- (mem rdx 0))
  (rcx *= 2)
  (rcx += 1)
  (rax <- (print rcx))))
