;; Print all multiples of 3 below 10
(((rsi <- 0)
  :loop
  (rcx <- rsi) ;; Convert
  (rcx *= 2)
  (rcx += 1)

  (rax <- (print rcx)) ;; Print

  (rsi += 3) ;; Increment counter
  (cjump rsi < 10 :loop :out)
  :out
  (rax <- rax)))
