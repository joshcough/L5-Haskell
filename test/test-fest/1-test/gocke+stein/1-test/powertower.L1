(((rax <- 2)
  (rbx <- 2)
  (rcx <- 2)

  :loop1
  :loop2
  (rax *= rax)
  (rbx -= 1)
  (cjump rbx = 0 :finish2 :loop2)
  :finish2
  (rbx <- 2)
  (rcx -= 1)
  (cjump rcx = 0 :finish1 :loop1)
  :finish1

  (rax *= 2)
  (rax += 1)
  (rax <- (print rax))))
