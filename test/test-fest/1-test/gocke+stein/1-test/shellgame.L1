(((rax <- 1000)
  (rbx <- 0)
  (rcx <- 1)
  (rdx <- 2)

  :loop
  (rdi <- rbx)
  (rbx <- rcx)
  (rcx <- rdx)
  (rdx <- rdi)
  (rax -= 1)
  (cjump rax = 0 :finish :loop)

  :finish
  (rbx *= 2)
  (rbx += 1)
  (rax <- (print rbx))))
