;; Array length
(((rax <- (allocate 7 11))
  (rdi <- rax)
  (rax <- (allocate 11 13))
  (call :sum)
  (rax *= 2)
  (rax += 1)
  (rax <- (print rax)))
 (:sum
  (rax <- (mem rax 0))
  (rsi <- (mem rdi 0))
  (rax += rsi)
  (return)))

