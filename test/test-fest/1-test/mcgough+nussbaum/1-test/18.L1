;;11
(((rax <- (allocate 3 3))
  ((mem rax 0) <- 21)
  (call :function1)
  (rax <- (mem rax 0))
  (rax <- (print rax)))
 (:function1 
  (rdx <- (mem rax 0))
  (call :function2)
  (return))
 (:function2 
  (rdx += 2)
  ((mem rax 0) <- rdx)
  (return)))