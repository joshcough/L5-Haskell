(((rsi <- 27) ;;put 13 encoded into rsi
(rsi >>= 1) ;; de-encode
(rdx <- 15) ;; put 7 encoded into rdx
(rdx >>= 1) ;; de-encode
(rsi += rdx) ;; add rdx to rsi
(rsi *= 2) ;; encode again
(rsi += 1)
(rax <- rsi)
(rax <- (print rax))))

