;;10
(((rax <- (allocate 3 3))
  (rcx <- rax)
  ((mem rcx 0) <- 21)
  (call :function1)
  (rax <- (mem rcx 0))
  (rax <- (print rax)))
 (:function1 
  (rdx <- (mem rcx 0))
  (cjump rdx < 23 :good :bad)
  :good
  (rax <- rdx)
  (goto :return)
  :bad
  (call :function2)
  (goto :return)
  :return
  (return))
 (:function2 
  (rdx += 2)
  (rax <- rdx)
  (return)))