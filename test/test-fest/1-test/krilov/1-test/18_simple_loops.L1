; Test simple loops
((
  (rdi <- 5)   ; loop termination variable
  (rsi <- 0)   ; loop index variable
  
  :loop
  (cjump rsi < rdi :keep_going :done)
  
  :keep_going  
  (rax <- (print 3))
  (rsi += 1)
  (goto :loop)
  
  :done
))
