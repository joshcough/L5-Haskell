; Test functions
((
  (call :func2)  
  (rax <- (print rax))
  
  (call :func3)
  (rax <- (print rax))
  
  (call :func1)
  (rax <- (print rax))
  )
 (:func1
   (rax <- 3)
   (return))
 (:func2
   (rax <- 5)
   (return))
 (:func3
   (rax <- 7)
   (return))
)
