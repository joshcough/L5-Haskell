; Test tail call recursion
((
  (rdi <- 3)   ; recur 3 times
  (call :rec)
  )
 (:rec
   (rax <- (print 3))
   (rdi -= 1)
   (cjump rdi <= 0 :done :recur)
   :recur
   (tail-call :rec)
   :done
   (return))
)
