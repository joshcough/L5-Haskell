; Test tail calls
((
  (call :func1)
  (rax <- (print rax))  ; 2
  )
 (:func1
   (rax <- (print 3))  ; 1
   (tail-call :func3))
 (:func2
   (rax <- 5)
   (return))
 (:func3
   (rax <- (print 7))  ; 3
   (tail-call :func2))
)
