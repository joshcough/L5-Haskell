; More cjump tests - conditional function calls from one procedure to another
((
  (rdi <- 3)
  (call :func1)
  )
 (:func1
   (rax <- (print 3))
   (rdi -= 1)
   (cjump rdi <= 0 :call_func2 :recur)
   :recur
   (tail-call :func1)
   :call_func2
   (tail-call :func2)
   (return))
 (:func2
   (rax <- (print 5))
   (return)
   )
)
