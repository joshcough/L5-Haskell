; Test functions calling other functions
((
  (call :func1)
  (rax <- (print rax))  ; 2
  )
 (:func1
   (rax <- (print 3))  ; 1
   (call :func3)
   (return))
 (:func2
   (rax <- 5)
   (return))
 (:func3
   (rax <- (print 7))  ; 3
   (call :func2)
   (return))
)
