; Test functions
((
  (call :func2)  
  (call :func3)
  (call :func1)
  )
 (:func1
   (rax <- 3)
   (rax <- (print rax))
   (return))
 (:func2
   (rax <- 5)
   (rax <- (print rax))
   (return))
 (:func3
   (rax <- 7)
   (rax <- (print rax))
   (return))
)
