; Test comparing literals to other literals
((
  (rax <- 3 = 3)  ; rax gets 1, but this is encoded, so the actual value is 0
  (rax <- (print rax))  ; 0
  
  (rax <- 3 = 4)  ; rax gets 0, but this is untagged...
  (rax += 11)     ; ... so add 11 to it, so now printing it out should show 5
  (rax <- (print rax))  ; 5
  
  (rax <- 5 < 7)
  (rax <- (print rax))  ; 0
  
  (rax <- 5 <= 6)
  (rax <- (print rax))  ; 0
  
  (rax <- 7 < 5)
  (rax += 11)
  (rax <- (print rax))  ; 5
  
  (rax <- 6 < 6)
  (rax += 11)
  (rax <- (print rax))  ; 5
  
  (rax <- 6 <= 6)
  (rax <- (print rax))  ; 0
  
))