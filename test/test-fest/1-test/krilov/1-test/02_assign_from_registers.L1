; Test assigning from one register to another
((
  
  (rcx <- 11)
  (rdx <- rcx)
  (rax <- (print rdx))   ; 5
  
  ; Test assigning from the same register
  (rcx <- 13)    ; pick a different number
  (rcx <- rcx)   ; assign from itself - value shouldn't change
  (rax <- (print rcx))   ; 6
))