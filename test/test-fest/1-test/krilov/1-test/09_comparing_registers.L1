; Test comparing registers to literals and other registers
((
  (rax <- 3)
  (rax <- rax = 3)
  (rax <- (print rax))  ; 0
  
  (rbx <- 4)
  (rax <- 3 = rbx)
  (rax += 11)
  (rax <- (print rax))  ; 5
  
  (rax <- 5)
  (rbx <- 7)
  (rax <- rax < rbx)
  (rax <- (print rax))  ; 0
  
  (rax <- 5)
  (rax <- rax <= 6)
  (rax <- (print rax))  ; 0
  
  (rbx <- 5)
  (rax <- 7 < rbx)
  (rax += 11)
  (rax <- (print rax))  ; 5
  
  (rbx <- 6)
  (rax <- rbx < rbx)
  (rax += 11)
  (rax <- (print rax))  ; 5
  
  (rax <- 6)
  (rax <- rax <= 6)
  (rax <- (print rax))  ; 0
  
))