(((rsi <- 21)
  (rax <- (allocate rsi 5))
  (rdi <- rax)
  (rbx <- rdi)
  (rbx += 20)
  ((mem rdi 4) <- 1)
  ((mem rdi 8) <- 3)
  ((mem rdi 12) <- 5)
  ((mem rdi 16) <- 7)
  ((mem rdi 20) <- 9)
  ((mem rdi 24) <- 11)
  ((mem rdi 28) <- 13)
  ((mem rdi 32) <- 15)
  ((mem rdi 36) <- 17)
  ((mem rdi 40) <- 19)
  (rcx <- (mem rdi 28))
  (rax <- (print rdi))))