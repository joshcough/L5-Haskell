(((rax <- 5)
  (rcx <- rax)
  (rcx -= 1)
  (call :bad)
  (call :complexity)
  (rax *= 2)
  (rax += 1)
  (rax <- (print rax)))
 (:bad
  :loop
  (rax *= rcx)
  (rcx -= 1)
  (cjump rcx = 0 :done :loop)
  :done
  (return))
 (:complexity
  (rax *= rax)
  (return)))