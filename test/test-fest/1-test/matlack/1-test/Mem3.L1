(((rsi <- 21)
  (rax <- (allocate rsi 5))
  (rdi <- rax)
  (rbx <- rdi)
  (rbx += 20)
  ((mem rdi 4) <- 5)
  (rax <- (mem rdi 4))
  (rax *= rax)
  (rax -= 5)
  (rcx <- rax)
  ((mem rdi 16) <- rax)
  (rcx *= rax)
  (rdx <- rcx)
  (rcx -= 1)
  ((mem rdi 20) <- rcx)
  (rdx -= 1)
  ((mem rdi 16) <- rdx)
  ((mem rdi 8) <- rdx)
  (rax <- (print rdi))))