(((rax <- 5)
  (rcx <- rax)
  (rcx -= 1)
  (call :fact)
  (rax *= 2)
  (rax += 1)
  (rax <- (print rax)))
 (:fact
  :loop
  (rax *= rcx)
  (rcx -= 1)
  (cjump rcx = 0 :done :loop)
  :done
  (return)))