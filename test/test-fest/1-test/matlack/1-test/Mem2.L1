(((rsi <- 21)
  (rax <- (allocate rsi 5))
  (rdi <- rax)
  (rbx <- rdi)
  (rbx += 20)
  ((mem rdi 4) <- 9)
  (rax <- 7)
  ((mem rdi 8) <- rax)
  (rax <- (mem rdi 4))
  ((mem rdi 12) <- rax)
  ((mem rdi 16) <- 15)
  ((mem rdi 20) <- 85)
  (rax <- (print rdi))))