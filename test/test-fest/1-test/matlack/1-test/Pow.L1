(((rax <- 4)
  (rbx <- rax)
  (rcx <- 13)

  :loop
  (cjump rcx = 1 :done :keep_going)
  :keep_going
  (rax *= rbx)
  (rcx -= 1)
  (goto :loop)
  :done
  (rax <<= 1)
  (rax += 1)
  (rax <- (print rax))))