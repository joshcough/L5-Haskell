(((rsi <- 21)
  (rax <- (allocate rsi 5))
  (rdi <- rax)
  (rbx <- rdi)
  (rbx += 20)
  ((mem rdi 4) <- 1)
  ((mem rdi 8) <- 3)
  ((mem rdi 12) <- 5)
  ((mem rdi 16) <- 7)
  ((mem rdi 20) <- 9)
  ((mem rdi 24) <- 11)
  ((mem rdi 28) <- 13)
  ((mem rdi 32) <- 15)
  ((mem rdi 36) <- 17)
  ((mem rdi 40) <- 19)
  
  (rax <- 10)
  (rcx <- 0)
  :loop
  (cjump rax = 0 :done :keep_going)
  :keep_going
  (rdx <- (mem rdi 12))
  (rcx += rdx)
  (rax -= 1)
  (goto :loop)
  
  :done
  (rcx <<= 1)
  (rcx += 1)
  (rax <- (print rcx))))