(((rax <- 85)
  (rbx <- 1)
  
  :loop
  (cjump rax = 0 :done :keep_going)
  
  :keep_going
  (rax -= 1)
  (rbx += 3)
  (goto :loop)
  
  :done
  (rcx <- rbx)
  
  :loop2
  (cjump rax < 42 :done2 :keep_going2)
  
  :keep_going2
  (rcx *= rbx)
  (rbx -= 1)
  (goto :loop2)
  
  :done2
  (rcx *= 2)
  (rcx += 1)
  (rax <- (print rcx))))
  