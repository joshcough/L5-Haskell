(((rax <- (allocate 5 5))
  (rsi <- rax)
  (rdi <- 10)
  (rax <- (print rsi))
  :loop
  (rax <- (mem rsi 0))
  (rax <<= 1)
  (rax += 1)
  (rax <- (allocate rax rax))
  (rsi <- rax)
  (rax <- (print rsi))
  (rdi -= 1)
  (cjump 0 < rdi :loop :exit)
  :exit
  (rax <- (print rsi))))
