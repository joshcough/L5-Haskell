(((rax <- (allocate 41 7))
  (rsi <- rax)
  (rax <- (mem rax 0))
  (rax *= 2)
  (rax += 1)
  (rax <- (print rax))
  (rax <- (print rsi))
  (rax <- (mem rsi 0))
  (rax -= 10)
  ((mem rsi 0) <- rax)
  (rax *= 2)
  (rax += 1)
  (rax <- (print rax))
  (rax <- (print rsi))))
