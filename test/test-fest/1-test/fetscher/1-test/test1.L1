(((rbx <- 5)
  (rcx <- rbx)
  (rax <- (print rcx))
  (rbx <- 5)
  (rbx *= 2)
  (rbx += 1)
  (rax <- (print rbx))
  (rbx <- 5)
  (rbx <<= 1)
  (rbx += 1)
  (rax <- (print rbx))
  (rbx <- -1)
  (rbx *= 2)
  (rbx += 1)
  (rax <- (print rbx))))
