(((rax <- (allocate 9 1))
  (rsi <- rax)
  (rax <- (mem rsi 0))
  (rax *= 2)
  (rax += 1)
  (rax <- (print rax))
  ((mem rsi 4) <- 5)
  ((mem rsi 8) <- 7)
  (rax <- (mem rsi 4))
  (rax <- (print rax))
  (rax <- (mem rsi 8))
  (rax <- (print rax))
  (rax <- (print rsi))
  ((mem rsi 16) <- 9)
  (rax <- (print rsi))))
