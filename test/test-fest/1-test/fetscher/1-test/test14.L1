(((rax <- 10)
  (rax <<= 1)
  (rax += 1)
  (rax <- (allocate rax 0))
  (rcx <- 1)
  :loop
  (rdx <- rcx)
  (rdx *= 4)
  (rdx += rax)
  (rbx <- rcx)
  (rbx *= rbx)
  (rbx *= 2)
  (rbx += 1)
  ((mem rdx 0) <- rbx)
  (rcx += 1)
  (cjump rcx <= 10 :loop :exit)
  :exit
  (rax <- (print rax))))
