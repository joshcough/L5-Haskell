(((rax <- (allocate 21 3))
  (rsi <- rax)
  (rdi <- 1)
  :loop
  (rax <- (print rsi))
  (rax <- rdi)
  (rax *= 4)
  (rax += rsi)
  (rcx <- (mem rax 0))
  (rax += 4)
  (rdx <- (mem rax 0))
  (rdx >>= 1)
  (rcx >>= 1)
  (rdx += rcx)
  (rdx <<= 1)
  (rdx += 1)
  ((mem rax 0) <- rdx)
  (rdi += 1)
  (cjump rdi < 10 :loop :quit)
  :quit))
