(((call :numberone)
  (rax <- (print rax)))
 (:numberone
  (call :numberthree)
  (call :numbertwo)
  (rax += 1)
  (return))
 (:numbertwo
  (call :numberthree)
  (rax <<= 1)
  (return))
 (:numberthree
  (rax <- 9)
  (return)))

