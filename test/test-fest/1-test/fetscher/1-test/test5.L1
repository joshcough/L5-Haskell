(((rax <- 1234567)
  (call :zeroit)
  :fakereturn
  (rax <- (print rax)))
 (:zeroit
  (rbx <- 0)
  (rax &= rbx)
  (rax += 1)
  (goto :return)
  (rax &= rbx)
  (goto :fakereturn)
  :return
  (return)))
