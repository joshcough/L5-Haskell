(((call :a)
  (rax <- (print rax)))
 (:a
  (cjump 1 < 2 :true :false)
  :true
  (call :b)
  (return)
  :false
  (call :c)
  (return))
 (:b
  (cjump 1 <= 1 :trueb :falseb)
  :trueb
  (call :d)
  (return)
  :falseb
  (call :e)
  (return))
 (:c
  (rax <- 0)
  (return))
 (:d
  (cjump 1 = 2 :trued :falsed)
  :trued
  (rax <- 0)
  (return)
  :falsed
  (rax <- 1)
  (return))
 (:e
  (rax <- 0)
  (return)))
