(((rax <- 1)
  (call :printone))
 (:printone
  (rsi <- rax)
  (rsi <<= 1)
  (rsi += 1)
  (rax <- (print rsi))
  (rax <- rsi)
  (rax >>= 1)
  (rax += 1)
  (call :skipone)
  (return))
 (:skipone
  (cjump rax < 10 :go_on :quit)
  :go_on
  (rax += 1)
  (call :printone)
  :quit
  (return))
 )
